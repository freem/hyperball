# makefile for code name "hyper ball"
################################################################################


################################################################################
.phony: all clean

all:
	@echo "Wow! there's nothing here!"

clean:
	@echo "There's nothing to delete because I'm lazy and want to push an incomplete makefile first!"
